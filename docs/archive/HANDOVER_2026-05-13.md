# Pursuit_Chase_v1 Handover (2026-05-13)

세션 종료 시점 정리 — 다른 환경에서 이어 진행 가능하도록.

## 현재 위치

**Phase**: RT-1 진행 중 (Hybrid Differential Game framework 코드 통합).

**브랜치**: `main`
**미커밋 작업**: 본 커밋에 포함

## 완료 사항 (정리)

### 학술 문서 (PLAN.md)

| 절 | 내용 | 상태 |
|---|---|---|
| §2.2 | F-16 Doghouse plot 제1원리 derive (envelope) | ✅ G0_a/b/c 통과 |
| §2.3 | ∇V_i closed-form (PN, Corner, LDT, YoYo) | ✅ G1_a-e 통과 |
| §2.4 | τ_i functions (Layer 1 hard + Layer 2 soft) | ✅ G2_a-d 통과 |
| §2.5 | V_advantage (ATA+AA) — RED TEAM 결과 한계 인정 | ⚠️ 진동 발생 (G3b FAIL) |
| §2.6 | **Hybrid Differential Game formalism** (★★★ 핵심) | ✅ 형식화 완료 |

### 코드 (tools/basis/)

- `envelope_f16.py` — doghouse plot 제1원리
- `gradient_approximators.py` — V_advantage, ∇V_i, optimal_control (smooth saturation)
- `tau_functions.py` — τ_pn, τ_corner, τ_yoyo, τ_LDT (28-obs 직접)
- `hji_solve_v2.py` — 초기 HJI solve (잘못된 settings 발견)
- `hji_solve_v3.py` — 정정된 HJI solve (medium accuracy + 31 checkpoints)

### 코드 (examples/pursuit_chase_v1/nodes/)

- `intents.py` — 초기 Intent dispatcher (deprecated, V_adv 로 대체했다가 다시 hybrid 로)
- `continuous_policy.py` — τ-blend + LUT + branch dispatcher 통합
- `branch_dispatcher.py` — **RT-1 신규** Hybrid game mode 선택기
- `custom_actions.py` — PursuitChaseOptimal BT 노드, history buffer

### HJI LUT (logs/hji/ — .gitignore 됨, 재생성 가능)

- `V6d_wez_v3.npz` — 60s horizon, 12⁶ grid, 8.15% capturable. ~307MB.
- 재생성: `python tools/basis/hji_solve_v3.py --grid 12 --time 60 --accuracy medium`

## 미완료 / 다음 단계

### RT-1 마무리 (즉시, ~1시간)

1. **continuous_policy.py 의 hybrid 모드 매치 검증**:
```bash
PURSUIT_POLICY_MODE=hybrid PURSUIT_CONT_LOG=logs/diag/cont_RT1.csv \
  python scripts/run_match.py --agent1 pursuit_chase_v1 --agent2 simple --rounds 1
```
   - 기대: ATA Q4 < 90° (현재 143° 보다 큰 개선), WEZ entry > 0
   - branch=GunEngagement/OffensivePursuit 발동 확인

2. **v11 baseline 측정** (한 번 안 한 RED TEAM F9):
```bash
python scripts/run_match.py --agent1 adaptive_eagle_v11_code --agent2 simple --rounds 10
```
   - 우리 target metric (HP gap, WEZ tick 수, ATA trajectory)

3. **BT API hdg 부호 stub 테스트** (RED TEAM F4 closure):
   - examples/test_hdg/ 에 hdg=8 고정 명령 stub agent
   - 5 tick 후 dx/dy 변화로 좌/우 검증
   - 결론을 memory 에 저장

### RT-2 (1-circle ∇V_3^*) — PLAN §2.6.3 의 V_3^* 명세 구현

```python
# tools/basis/gradient_approximators.py 에 추가
def grad_V_1circle(x, alt_ft):
    """Shaw 정리 6 1-circle mode — R 최소화 추구."""
    V_p = x[4]
    R_now = env.turn_radius_ft(V_p, alt_ft)
    R_min = min over V of turn_radius_ft(V, alt_ft)  # envelope minimum
    err = R_now - R_min
    # ∂R/∂V_p — envelope_f16 derive 또는 numerical
    grad = np.zeros(6); grad[4] = ...
    return V, grad
```

τ_corner 의 1c sub-mode (HCA<90°) 활성 시 V_1c 사용. v11 의 implicit 1c/2c 구분의 형식화.

### RT-3 (SMT verification + LUT cross-check)

1. **G2_d Cover proof (SMT)**: dReal/Z3 로 ∀x in canonical_neighborhood, Σ τ_m(x) > ε
2. **∇V_i ↔ LUT 정합** (∇V_corner 가 LUT 의 high-τ_corner 영역에서 일치하는지)
3. **STL falsification** (VERIFICATION_METHODOLOGY.md §2.2 참조)

### 기타 누락 (deferral 금지 원칙 — 처리해야)

- **R5 정적 분석 S1-S10** (PLAN §2.2.8 등 명세 후 미실행)
- **R4 V_e marginalize** (closure 기반 추정, LUT 사용 시만)
- **R6 단일 매치 종합 검증** (RT-1 완료 시 자연 포함)

## 사용자 합의된 핵심 원칙 (메모리 저장됨)

1. **채점 기준 불변** (`feedback_never_change_scoring.md`) — WEZ/HP/verification gate 약화 금지
2. **결점 deferral 금지** (`feedback_no_deferral.md`) — 발견 즉시 fix
3. **F-16 envelope alt-dependent** (`project_envelope_altitude_finding.md`) — V_corner(15000ft)=438, ≠ v11 8000ft 의 350
4. **Pursuit_Chase_v1 Level-1 진단** (`project_pursuit_chase_L1.md`) — HJI-LUT quantize 결함 식별

## RED TEAM 발견 (PLAN §2.6.0 + 본 문서 §RT-1)

10 결함 + 4 전략 결함. 핵심:
- F1: v11 작동 구조 버린 것이 큰 후퇴 → RT-1 에서 복원 중
- F2: 6D state 가 vertical 못 봄 → RT-2 가 1c/2c 추가, RT-3 가 7D 검토
- F3: 12⁶ grid 의 fundamental 한계 → v3 LUT 가 가장 dense 했지만 여전 부족
- F9: v11 baseline 측정 안 함 → RT-1 immediate

## 환경 메모

- Python 3.14.3 (system)
- JAX CPU only (GPU 없음 — 12⁶ LUT 30분-1시간)
- Windows + WSL bash mixed
- 작업 디렉토리: `c:\Users\Joon\Desktop\AI-pilot\ai-combat-sdk`

## 다른 환경에서 이어가기

```bash
git pull
cd ai-combat-sdk
# 환경 재구성
python -m pip install hj-reachability --no-deps
python -m pip install flax --no-deps
# LUT 재생성 (30-60분)
python tools/basis/hji_solve_v3.py --grid 12 --time 60 --accuracy medium
# RT-1 시작
PURSUIT_POLICY_MODE=hybrid python scripts/run_match.py --agent1 pursuit_chase_v1 --agent2 simple --rounds 1
```
