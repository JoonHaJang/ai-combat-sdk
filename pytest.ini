[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (require simulation, deselect with -m "not slow")
    integration: marks tests as integration tests
addopts = -v --tb=short
